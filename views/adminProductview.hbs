
<body>
    <div class="container my-5">
        <h1 class="text-center mb-4">{{product.name}}</h1>
        <div class="card mx-auto" style="max-width: 600px;">
            <img src="/uploads/{{product.image}}" class="card-img-top" alt="{{product.name}}">
            <div class="card-body">
                <h5 class="card-title">{{product.name}}</h5>
                <p class="card-text"><strong>Brand:</strong> {{brandname}}</p>
                <p class="card-text"><strong>Price:</strong> ₹ {{product.price}}</p>
                <p class="card-text"><strong>Category:</strong> {{categoryName}}</p>
                <p class="card-text"><strong>Description:</strong> {{product.description}}</p>
                <p class="card-text"><strong>Created Date:</strong> {{createdAtDate}}</p>
                <p class="card-text"><strong>Updated Date:</strong> {{updatedAtDate}}</p>


                <a href="/admin/product" class="btn btn-secondary mt-3">Back to Products</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
